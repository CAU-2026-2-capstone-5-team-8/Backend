spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:backend}
    username: ${POSTGRES_USER:backend}
    password: ${POSTGRES_PASSWORD}

server:
  address: 127.0.0.1
