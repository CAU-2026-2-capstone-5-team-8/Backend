-- Flyway creates the dedicated schema before applying versioned migrations.
-- Business tables are introduced with their corresponding feature stages.
COMMENT ON SCHEMA backend IS 'CAU capstone team 8 backend application data';
